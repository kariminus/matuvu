{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <p>{{ app.user.firstname }}</p>
        <p>{{ app.user.lastname }}</p>
    </div>
    {% block link %}{% endblock %}

    {% block content %}{% endblock %}

    <div><a href="{{ path('security_logout') }}">Deconnexion</a></div>
{% endblock %}