{% extends 'PlatformBundle::layout.html.twig' %}
{% block headbar %}
<<<<<<< HEAD
    <h1>Propositions en attente</h1>
{% endblock %}

{% block link %}
    <div>
        <a href="{{ path('user_observations') }}">Accéder aux propositions</a>
    </div>
    <div>
        <a href="{{ path('user_edit') }}">Modifier mon profil</a>
=======
    <div class="headbar">
        <a href="javascript:history.back()" class="back"><</a>
        Propositions en attente
>>>>>>> karim
    </div>
{% endblock %}


<<<<<<< HEAD
{% block content %}
    <ul>
        {% for observation in observations %}
            <li><a href="{{ path('observation_view', {'id' : observation.id}) }}">{{ observation.oiseau.name }}</a></li>
        {% endfor %}
    </ul>
    <div class="navigation">
        {{ knp_pagination_render(observations) }}
    </div>

=======

{% block content %}
    <div class="border border-left col-md-8">
        <div class="list-observations">
            <ul>
        {% for observation in observations %}
            <li class="observation"><a class="lien-observation" href="{{ path('observation_view', {'id' : observation.id}) }}">{{ observation.oiseau.name }}</a></li>
        {% endfor %}
    </ul>
        </div>
    <div class="navigation">
        {{ knp_pagination_render(observations) }}
    </div>
    </div>
    <script>
        $(document).ready(function() {
            if (window.matchMedia("(max-device-width: 992px)").matches) {
                $(".left").hide();

            } else {
                $(".left").show();
            }
        });
    </script>
>>>>>>> karim
{% endblock %}