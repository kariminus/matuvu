<div>
    <a href="{{ path('image_delete', {'id' : observation.id}) }}">Supprimer l'image</a>
</div>
<div>
    <a href="{{ path('observation_delete', {'id' : observation.id}) }}">Refuser</a>
    <a href="{{ path('observation_validate', {'id' : observation.id}) }}">Valider</a>
</div>